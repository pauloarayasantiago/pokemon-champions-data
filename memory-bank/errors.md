# Errors Log

| Error | Date | Fix |
|---|---|---|
| Moves returning 0 results | 2026-04-12 | Move links use `href="/attackdex-champions/"` in `td.fooinfo` with `rowspan="2"`. Fixed parser to target these specifically instead of `cells[0]` |
| Type2 contaminated by move types | 2026-04-12 | `parse_types()` was matching ALL type images on page including moves. Fixed to only match type images inside links to `/pokedex-champions/{type}.shtml` |
| New abilities returning 0 | 2026-04-12 | Page uses `class="tab"` not `class="dextable"`. Fixed selector |
| Mega abilities returning 0 | 2026-04-12 | Same `class="tab"` issue, plus cell layout was 6 cells not 5. Fixed both |
| UnicodeEncodeError cp1252 | 2026-04-12 | Windows console issue. Use `PYTHONIOENCODING=utf-8` |
| Items/moves including header row | 2026-04-12 | Added `name != "Name"` filter to skip header rows parsed as data |
| Mega data incomplete (23 vs 59) | 2026-04-12 | `mega_abilities.csv` only lists megas with NEW abilities. Need to extract all megas from individual Pokémon pages where base + mega forms coexist |
| youtube-transcript-api API change | 2026-04-12 | v1.2.4 changed from `YouTubeTranscriptApi.get_transcript()` class method to `YouTubeTranscriptApi().fetch()` instance method returning `FetchedTranscript` with `.text` snippets |
| YouTube emoji in titles crash | 2026-04-12 | Windows cp1252 console can't encode emoji. Fixed with `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` |
| yt-dlp duration is float | 2026-04-12 | Video duration from yt-dlp metadata is float, not int. Added `int(duration)` cast before formatting |
| YouTube IP rate limit | 2026-04-12 | After ~24 transcript fetches, YouTube blocks IP. No documented cooldown — community reports suggest 1-24 hours. Scraper auto-skips already-downloaded videos on re-run |
| Base stats parser returning None | 2026-04-12 | `parse_base_stats()` checked `rows[0]` for "HP"/"Attack"/"Speed" headers, but Serebii puts a fooevo "Stats" label in row 0 and column headers in row 1. Fixed to match on fooevo "Stats" text and read data from row 2. Same fix for `parse_mega_stats()` (matches "Stats -") |
| Mega stats name matching failure | 2026-04-12 | `parse_mega_stats()` tried to return `mega_name` from stat table, but Serebii mega stat tables don't contain the mega name. Fixed by matching megas to stats by index order (both appear in same page order) |
| Floette missing base stats | 2026-04-12 | Floette-Eternal has a different page layout on Serebii — stat table header doesn't match "Stats" exactly. 1/186 affected, not critical |
| Pikalytics meta tag case | 2026-04-12 | `parse_usage_rank()` searched for `name="description"` but Pikalytics uses `name="Description"` (capital D). Fixed to match capital D |
| Pikalytics non-English moves | 2026-04-12 | Some Pokemon's move names appear in Italian. **Fixed in Phase 8 (2026-04-12)**: added `Accept-Language: en-US,en;q=0.9` header to `scraper_pikalytics.py` — the canonical structural fix. Originally also shipped a 2,383-entry IT→EN dictionary as a belt-and-braces chunk-time translator; **dropped in Phase 1 (2026-04-21)** as dead code once the header alone was verified sufficient across all re-scrapes |
| Mr. Rime no Pikalytics page | 2026-04-12 | No working URL slug found for Mr. Rime on Pikalytics (tried: Mr.%20Rime, Mr.Rime, Mr-Rime, MrRime, mr-rime). Added to SLUG_OVERRIDES as None (skip) |
| Usage chunks ranked low for popular Pokemon | 2026-04-12 | All 80 usage chunks share boilerplate text "competitive usage statistics: Ranked #X with Y%..." — MiniLM-L6-v2 weights this shared vocabulary over the single Pokemon name token. Fixed with over-fetch + metadata re-rank in `lib/rag.ts`. Boost gated on `wantsUsage` detection to avoid regressions |
| extractPokemonFromQuery TypeError | 2026-04-12 | `metadata.name` can be non-string (e.g., numeric from CSV). Casting `as string` then calling `.toLowerCase()` crashed. Fixed with `typeof raw !== "string"` guard |
| Protect move ranked low with EmbeddingGemma | 2026-04-14 | After embedding upgrade (Phase 5), query "how does Protect work in Champions" returned research docs above `move:protect`. The stronger 768-dim model weighted broader semantic context over exact move data. Fixed by adding move name dictionary + exact move name boost (+0.04) to re-ranker in `rag.ts` |
| EmbeddingGemma fp16 not supported | 2026-04-14 | `onnx-community/embeddinggemma-300m-ONNX` does NOT support fp16 dtype. Must use fp32, q8, or q4. Set `dtype: "q8"` in `lib/embed.ts` |
| esbuild ?? and \|\| operator precedence | 2026-04-13 | `data.ts:76`: `override?.type2 ?? row.type2?.trim() || null` fails — `Cannot use "\|\|" with "??" without parentheses`. Fix: wrap in parens `(row.type2?.trim() || null)` |
| Mega Garchomp defaulting in CLI | 2026-04-13 | `scripts/calc.ts`: `findMega("Garchomp")` was resolving Mega Garchomp even without `--mega` flag. Fix: only check mega when `isMega` flag is true or name starts with "Mega " |
| BITE_MOVES not defined | 2026-04-13 | `damage.ts:247`: `ReferenceError` during matrix build — missing import. Fix: added `BITE_MOVES` to import from `./data.js` |
| Rotom-Wash / Alolan Ninetales not in CSV | 2026-04-13 | Test used form names that don't exist in pokemon_champions.csv (just "Rotom", "Ninetales"). Fix: changed tests to use base names with explicit ability overrides |
| Rotom forms added to CSV (RESOLVED) | 2026-04-15 | All 5 Rotom appliance forms (Wash/Heat/Frost/Fan/Mow) added as separate rows in pokemon_champions.csv with correct types, stats (50/65/107/105/107/86 BST 520), and move pools (base moves + signature). Pikalytics re-scraped (Wash 16% #10, Heat 2% #43). Matchup + efficiency matrices rebuilt. Alolan Ninetales still outstanding. |
| Mega Charizard X/Y Map collision (RESOLVED) | 2026-04-16 | Both Mega Charizard X and Y were named "Mega Charizard" in `mega_evolutions.csv`, causing Map key collision in `getMegas()` — Y overwrote X, making Mega Charizard X completely inaccessible in damage calc. Fixed by renaming to "Mega Charizard X" / "Mega Charizard Y" in CSV + adding prefix matching in `findMega()` for backward compat. |
| Duplicate tournament team PC99 (RESOLVED) | 2026-04-16 | PC99 and PC132 were identical teams (same player, same replica code SNE72HTH6T). Removed PC99 from `tournament_teams.csv`. |
| /search 500 on Vercel — onnxruntime native bindings (RESOLVED) | 2026-04-18 | `@huggingface/transformers` loads `onnxruntime-node` `.node` binaries at module-eval time; Vercel Lambda's bundler doesn't ship them reliably. Fixed by lazy-importing the pipeline and routing query embeds to HF Inference API when `HF_TOKEN` is set. Local indexing scripts keep the bundled path. |
| Pikalytics scraper missed form variants (RESOLVED) | 2026-04-20 | `pikalytics_usage.csv` had only base names. Pikalytics actually serves per-form pages (`Rotom-Wash`, `Ninetales-Alola`, `Tauros-Paldea-Aqua`, Hisuian/Galarian forms, `Floette-Eternal`, `Aegislash-Blade`, `Meowstic-F`). Scraper iterates `pokemon_champions.csv`, so it auto-picked up all variants once the 30 new rows were in the CSV. Result: 91 Pokemon (was 82), 10 new form-variant entries. Notable: Floette-Eternal #7 at 19%, Rotom-Wash #8 at 18%. 4 forms legitimately 404 (Basculegion-F, Palafin-Hero, Lycanroc poses, Gourgeist sizes — Pikalytics treats them as single species). |
| Matchup/efficiency matrices missing Megas (RESOLVED) | 2026-04-20 | `lib/calc/data.ts readCSV()` filtered rows on `r.name && r.name.trim().length > 0` — but `mega_evolutions.csv` / `mega_abilities.csv` first column is `base_pokemon` / `pokemon`, not `name`. Every mega row was silently dropped, yielding `getMegas().size === 0` and matrices that only covered base Pokemon. Fixed by filtering on the first column regardless of name. Matrices rebuilt: 75,350 rows, 275 attackers (216 Pokemon + 59 Megas), all 30 form variants present. |
| Scraper regenerates ambiguous "Mega Charizard" names (RESOLVED) | 2026-04-20 | Serebii lists both Mega Charizard X and Y under the same "Mega Charizard" label; the scraper wrote both as identical rows, wiping the Apr 16 X/Y rename fix on every re-scrape. Fixed structurally in `scraper.py main()` — when a Pokemon has multiple megas with the same scraped name, the emit loop appends " X" / " Y" suffixes in page-order. Manually re-patched `mega_evolutions.csv` for the current run. |
| Serebii scraper wipes manual variant rows (RESOLVED) | 2026-04-20 | `scraper.py` wrote `pokemon_champions.csv` from scratch, erasing 15 manually-added regional / Rotom / Tauros-breed rows on every re-scrape. Fixed by adding a `FORM_VARIANTS` dict (21 base Pokemon → 30 variant specs: 12 regional, 5 Rotom, 3 Paldean Tauros, 10 other forms) plus `parse_section_moves()` / `parse_section_stats()` helpers that pull alt-form move lists and stats from the same page's h2/h3 sections. `scrape_pokemon()` now emits `variants` list, `main()` writes a row per variant. Output: 216 rows natively (186 base + 30), survives all future re-scrapes. |
| /search "Search failed" card on Vercel — legacy HF endpoint 404 (RESOLVED) | 2026-04-18 | `api-inference.huggingface.co/pipeline/feature-extraction/{model}` returns 404 for `sentence-transformers/all-MiniLM-L6-v2` after HF consolidated serverless inference behind the Inference Providers router. Fixed by switching `lib/embed.ts` URL to `https://router.huggingface.co/hf-inference/models/{model}/pipeline/feature-extraction` (commit `57ff6f4`). Also added `AbortSignal.timeout(8000)` + single 503 retry, `maxDuration = 30` on `src/app/search/page.tsx`, and gated `checkStaleness()` off on Vercel since Lambda mtimes never match reindex-time mtimes (the warning was always a false positive in prod). |
| Jina rerank 403 + unhandled DOMException kills eval run early (RESOLVED) | 2026-04-22 | Jina balance is depleted → `rerankCandidates()` in `lib/rerank.ts` gets 403 and returns null as designed. But the `AbortSignal.timeout(JINA_TIMEOUT_MS)` timer is still pending; when it fires after the fetch has already completed, Node emits an unhandled `DOMException [TimeoutError]` from `process.processTimers` that terminates the eval process mid-run (exit 0, but only the first 1-2 tests completed). **Workaround for Phase 2 eval runs:** run with `JINA_API_KEY=` to skip the Jina path entirely — there's no rerank fetch, so no race. Required an upstream fix to `scripts/eval-models.ts loadEnv()` (next row) for the env override to actually stick. Long-term, replace `AbortSignal.timeout()` with a manual `AbortController` + `clearTimeout()` on the Jina client. |
| eval-models.ts loadEnv() clobbers explicit shell env (RESOLVED) | 2026-04-22 | `loadEnv()` unconditionally wrote every `.env` key into `process.env`, so `JINA_API_KEY= npx tsx scripts/eval-models.ts` was silently overridden by the `.env` value. Fixed in Phase 2 commit `bc02d11`: `loadEnv()` now checks `Object.prototype.hasOwnProperty.call(process.env, key)` and skips if the key was already set (even to empty string). Standard `.env` loader behavior. |
| Gemma collapses to `{"claims": []}` when citation validator nudges (RESOLVED) | 2026-04-22 | Phase 2 v4 prompt told the model "If you made no search-backed factual claims, emit `{\"claims\": []}`" as a valid fallback. When the server-side validator rejected an invalid `chunk_id` and nudged the model to re-ground or remove, Gemma chose the easier escape: emit empty claims instead of re-grounding on a valid ID. Observed on `creator_opinion` test in Phase 2 run 1 — model made real factual claims in prose (Garchomp usage #3, 38.0% tournament rate) but emitted `claims: []`. **Fixed in v4.1 prompt** (commit `bc02d11`): rule 2 now says "EVERY search-backed factual statement needs a claim entry — not just the 'main answer', every supporting/contextual fact too"; rule 5 explicitly forbids "do NOT collapse to an empty claims array to avoid the validator". Retry-nudge text tightened similarly. Result: citation_validity_rate 80% → 100% on subsequent runs. |
| Planner × reranker score-merge regresses matchup nDCG (BLOCKED-pending-Phase-5) | 2026-04-22 | Phase 3 attempted Gemma 4 26B pointwise rerank then BAAI/bge-reranker-base via HF Inference. Both regressed matchup nDCG by 15-18% (gate ≥0.77, measured 0.629/0.605) despite working correctly on passthrough queries (`move-protect` smoke: grade-3 chunk moved from rank 5 → rank 2 under cross-encoder). **Root cause:** Stage 6.3's executor reranks each sub-query independently then max-merges. Sharp reranker scores create disparities — same chunk gets 0.95 from "left: garchomp moveset" sub-query but 0.4 from "original: Garchomp vs Charizard". Max-merge keeps 0.95 but other "wrong" chunks also tie at 0.95 from different sub-queries; boost layer can't differentiate post-merge. Adversarial regressed too (force-included banned-item chunks scored low by reranker). **Workaround for now:** code stays in [lib/rerank.ts](../lib/rerank.ts) behind `RERANKER` env var (default falls through to no-op — identical to Phase 2 nDCG=0.851). **Long-term fix:** Phase 5 (executor redesign) re-applies `collectForceIncludes()` and `applyBoosts()` post-merge against the user's ORIGINAL query, eliminating sub-query score interference. Phase 3 retry: post-Phase 5, flip `RERANKER=crossencoder`. Snapshots: `retrieval-phase3-{gemma,crossencoder}.json`. Plan: `~/.claude/plans/read-the-memory-bank-plan-peaceful-pumpkin.md`. |
| Pointwise LLM rerank score compression (BLOCKED) | 2026-04-22 | Gemma 4 26B doing pointwise relevance scoring `{"score": 0.X}` per candidate gives compressed scores in 0.4-0.7 range — can't distinguish "this IS the answer" from "this MENTIONS the topic". Symptom: `move-protect` baseline rank 1 (`moves.csv`) slipped to rank 5 with Gemma; `champions_rules.md` (which mentions "Protect nerfed to 8 PP" tangentially) got rank 1 instead. The boost layer (calibrated for sharp 0.95-vs-0.05 reranker spreads like Jina's) can't recover from compressed signals. **Fix:** specialized cross-encoders (BGE) give the sharp spread the boost layer expects — confirmed via 5-pair HF probe (scores 0.0001 → 0.27 → 0.99). However cross-encoder THEN exposed the planner × reranker score-merge problem (separate row above). General-purpose LLMs are not viable as pointwise rerankers for this stack — use a specialized reranker. |
| Jina reranker fired silently by default → wasted 300-500ms per RAG call (RESOLVED) | 2026-04-23 | `lib/rag.ts` line 222-223 default fallback was `(process.env.RERANKER ?? "").toLowerCase() \|\| (GEMMA_RERANK_ENABLED === "true" ? "gemma" : "jina")`. With RERANKER unset and GEMMA_RERANK_ENABLED unset (true defaults across the project), dispatch routed to Jina on every request. Jina account has been depleted for months, so the call errored 403, fell back to no-rerank — but still cost ~300-500ms of network round-trip + timeout per RAG call. Observed during 7-model agentic eval runs (Jina 403 log spam). Fixed by changing the default from `"jina"` to `"none"`. Matches what `activeContext.md` already claimed the default was. Retrieval baseline 0.853 unchanged (Jina was failing silently anyway). |
| phantom_pokemon fails on every LLM tested — systemic, not a Gemma flake (RESOLVED) | 2026-04-23 | 7-model bake-off (Gemma 4 26B, Groq Llama 3.3 70B, GPT-OSS 20B, qwen2.5-7b, llama3.1-8b, DeepSeek V3.2, GLM-4.5-Air, Gemini 2.5 Flash Lite) all failed `phantom_pokemon` at ~1/3-3/3 rate. Every model sees "Amoonguss" or "Porygon2", answers from training data without calling `pokedex`. Master plan's A4 was originally framed as a Gemma flake fix — session 2026-04-23 reframed as model-agnostic. Fixed via pre-flight interceptor in `lib/phantom-guard.ts` + wiring in `src/app/api/team/route.ts` (POST handler, post-meta/pre-loop) and `scripts/eval-models.ts` (`runAgent()`). Uses PRE_EVO_MAP (23 entries, exported from `lib/team-validator.ts`) + small EXPLICIT_PHANTOMS table (Amoonguss today; extensible). Word-boundary match with hyphen-aware lookarounds so "porygon" inside "porygon-z" doesn't match. |
